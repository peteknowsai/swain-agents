# 2026-02-08 Session Notes

## Library Status
- Library grew from ~100 visible cards to **200 cards** across all locations
- 58 cards were incorrectly marked "timely" (with expiration dates) that should be evergreen — fixed all 58 in a batch update
- 45 cards had placeholder images (placehold.co or unsplash) — dispatched 5 stylist sub-agent batches to generate proper images

## Content Production: 50-Card Batch
Dispatched ~50 new cards in one session. Key additions:
- **Tampa Bay / Tierra Verde**: Shell Key guide, Egmont Key guide, Port32 TV welcome, Port32 Tampa welcome, Skyway Bridge fishing, Bunces Pass & Fort De Soto, sandbar guide, sunset spots, Hula Bay to Gandy waterfront run, Tampa Bay Weekend day plans, Top 10 fishing spots, channel & ramp guide
- **Maintenance (Florida-level)**: Salt flush, bottom paint 101, outboard engine care, spring commissioning checklist, trailer boat maintenance — went from 3 to ~20 cards
- **Lifestyle (NEW category)**: Sandbar day packing guide, raft-up etiquette, kids on board, sunset cruise planner SW FL, full moon night cruise, overnight anchoring guide, best weekend trips SE FL, best weekend trips SW FL — 0 to 9 cards
- **Events**: Miami Boat Show (Feb 12-16), Tampa Bay Boat Show (Mar 6-8), Palm Beach Boat Show, Crystal Coast tournament calendar
- **Safety (Florida-level)**: VHF radio channels, anchoring 101, lightning safety, man overboard drill, engine failure guide
- **Fishing (Florida-level)**: Sheepshead season, snook season opens March 1, live bait guide, catch & release, grouper season guide, kayak fishing
- **Regulations**: Manatee zones, boat insurance 101

## Coverage After Batch
| Location | Cards |
|----------|-------|
| Tierra Verde | 42 |
| Florida (state) | 23 |
| Tampa Bay | 21 |
| Jacksonville | 13 |
| Naples | 12 |
| Lighthouse Point | 12 |
| Fort Lauderdale | 12 |
| Palm Beach Gardens | 11 |
| Cape Coral | 11 |
| Morehead City | 10 |
| Marco Island | 10 |

## Stylist Sub-Agent Pattern (PROVEN)
- **Stylist is an OpenClaw sub-agent**, NOT a Claude Agent SDK agent
- Dispatched via `sessions_spawn` with specific image prompts per card
- Workflow: nanobanana -c --json → image tool (Haiku vision) for bg color → skip card update
- ~70 seconds per card, runs on Opus
- Can batch 9 cards per sub-agent, run 5 parallel = 45 cards in ~10 min wall time
- I write custom image prompts matched to card content/mood — this is editorial control
- Successfully styled all 45 placeholder cards in one run (5 batches × 9 cards)

## Style Library Usage (ACTION NEEDED)
- Pete flagged: I'm NOT using the 133-style library when dispatching stylists
- Style library has `promptText` per style that should inform the image generation
- Built a curated cheat sheet at `memory/style-cheatsheet.md` — ~30 go-to styles organized by content type
- **Going forward**: I pick a style from the cheat sheet, write a custom prompt channeling that style's vibe, tell stylist to set `--style-id` on the card update
- Pete's vision: style library = "style suggestions, snippets" — not rigid templates
- Current 45 cards have images but no style assignments — can retroactively assign style IDs without regenerating images
- "More diversity of styles" — don't default to the same 5, use the full range

## Architecture Decisions
- **Beat reporters** stay on Claude Agent SDK (Sonnet, lean, server handles card lifecycle)
- **Stylist** is OpenClaw sub-agent (needs exec for nanobanana + image tool for vision)
- **Me (Mr. Content)** dispatches both, reviews quality, makes editorial calls
- Pipeline: I dispatch reporters → they create cards with placeholder images → I dispatch stylist sub-agents → cards fully styled

## NanoBanana
- Uses **Gemini** for image generation (not Replicate)
- `-c` flag uploads directly to Cloudflare Images
- `--json` flag returns structured output with URL
- Server's `regen-image` endpoint was broken (hitting Replicate with expired token) — bypassed entirely with direct CLI calls via sub-agents
- Replicate token was expired on Feb 8 — Pete fixed it but we don't need it anymore since we use Gemini path

## Cron Jobs
- All 19 cron jobs are enabled but NONE have ever fired (zero run history)
- Gateway was likely not running when scheduled times hit
- Not a priority — this is dev env, we do things manually
- Daily news sweeps (6 markets), weekly magazine/fishing/events scans, daily advisor briefings, monthly strategy review all wired up for when we go live

## Category Issues Found
- Some reporters created non-standard categories: `port32-marinas`, `safety-equipment`, `Lifestyle` (capital L), `activities-events`, `guide` — these should be normalized to the canonical 10
- Canonical categories: weather, fishing, safety, destinations, dining, events, maintenance, regulations, port32, lifestyle
- TODO: batch fix these non-standard categories

## Agents Created This Session
- `beat-lifestyle-florida` — state-level lifestyle content
- `beat-lifestyle-tampa-bay` — Tampa Bay lifestyle
- `beat-lifestyle-sw-florida` — SW Florida lifestyle
- `beat-lifestyle-se-florida` — SE Florida lifestyle
- `beat-fishing-florida` — state-level fishing content (sheepshead, grouper season, bait guide, etc.)

## Reporter Quality Notes
- **Dining cards** come back excellent — best voice, most personality (e.g., "grouper sandwich that's better than it needs to be")
- **Destinations cards** are more generic/guidebook-y — need more specific local detail in the dispatch instructions
- **Fishing run for Tampa Bay** completed but did NOT create a new card — "ghost completion" issue, run said success but no card. Investigate.
- Beat reporters need `--instructions` flag or they fail
- If no agent exists for a beat+location combo, `skip beat run` returns 400 — need to `skip beat create` first

## End of Day Summary
- 200 cards in library (up from ~100 visible at start of day)
- 50 new cards produced
- 58 freshness fixes (timely → evergreen)
- 45 images generated via 5 parallel stylist sub-agents
- Style cheat sheet built at `memory/style-cheatsheet.md`
- All cron jobs wired but not firing in dev — not a priority

## Pete's Direction
- "Don't go hog wild" — do a few, check quality, then scale
- Reporters should stay as Agent SDK agents, stylist as sub-agent
- "Too many layers" — keep things simple, use nanobanana CLI directly
- Prefers me to have editorial control over image prompts
- Wants expired evergreen content identified and fixed
- 50-card batch approved after reviewing the plan
- **Use the style library** — pick styles from the library, write custom prompts channeling the style's vibe, set --style-id on cards
- Style library should be treated as "style suggestions, snippets" — not a rigid template system
- Wants more diversity of styles than what we're getting

---
name: swain-boat-art
description: Generate daily boat art for captains — personalized art of their boat in different styles.
metadata: { "openclaw": { "emoji": "🎨", "requires": { "bins": ["swain"] } } }
---

# Boat Art

Every captain gets a new piece of art featuring their boat, every single day. This is a signature Swain feature.

## How It Works

The `swain card boat-art` command generates stylized art of the captain's boat and creates a card automatically.

**If the captain has a boat photo on file** — the art is generated by restyling their actual boat photo (image-to-image). The boat stays recognizable but gets transformed into the chosen art style.

**If no photo yet** — the art is generated from a text description based on their boat name, type, make/model, and marina. Still looks great, but a real photo makes it better.

## Available Styles

- `watercolor` — Soft flowing colors, gallery quality
- `oil-painting` — Rich impasto brushstrokes, classical maritime art
- `pop-art` — Bold colors, Andy Warhol inspired
- `japanese-woodblock` — Ukiyo-e style, Hokusai inspired
- `impressionist` — Monet-inspired water reflections
- `comic-book` — Bold ink outlines, halftone dots
- `art-deco` — 1920s poster aesthetic
- `minimalist` — Clean lines, limited palette
- `sunset-silhouette` — Dramatic silhouette against vibrant sky
- `neon` — Glowing neon outlines on dark background

## Commands

### Generate a single art card (random style)
```bash
swain card boat-art --user={{userId}} --json
```

### Generate a specific style
```bash
swain card boat-art --user={{userId}} --style=watercolor --json
```

### Generate the 6-style sampler (onboarding only)
```bash
swain card boat-art --user={{userId}} --sampler --json
```
This creates 6 separate cards — one in each sampler style (watercolor, oil-painting, pop-art, japanese-woodblock, impressionist, comic-book).

## Onboarding — The Art Sampler

During the **first briefing**, include a boat art sampler to introduce the feature:

1. Run `swain card boat-art --user={{userId}} --sampler --json`
2. This creates 6 cards showing their boat in different styles
3. Include all 6 cards in the briefing with a text item explaining the feature:
   - "One of my favorite things I do for you — every day, I create a new piece of art featuring [boat name]. Here's a taste of what's coming. Eventually you'll be able to print these too."
4. Let them see the variety. They'll look forward to it daily.

## Daily Briefings — One Art Card Per Day

Every daily briefing should include **one boat art card**:

1. Run `swain card boat-art --user={{userId}} --json` (random style)
2. Include the card in the briefing with a brief commentary
3. Rotate through styles — try not to repeat the same style two days in a row

## Getting a Boat Photo

A real photo makes the art dramatically better. Ways to get one:

- **Ask during onboarding**: "Got a pic of [boat name]? I use it to make custom art for you every day."
- **Ask in the first briefing**: Include a text item asking for a photo
- **They may send it via WhatsApp** — when they do, save it:
  ```bash
  swain user upload-boat-image {{userId}} --url=<photo_url>
  ```
  or if you have the file locally:
  ```bash
  swain user upload-boat-image {{userId}} --file=<path>
  ```

Once a photo is on file, all future boat art automatically uses it for image-to-image generation.

{
  "name": "swain-cli",
  "version": "0.7.3",
  "type": "module",
  "scripts": {
    "generate-skills": "bun run lib/skills-gen.ts",
    "build": "bun run generate-skills && bun build swain.ts --compile --outfile bin/swain --define \"process.env.BUILD_SHA='$(git rev-parse --short HEAD)'\" --define \"process.env.CLI_VERSION='$(jq -r .version package.json)'\"",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "yaml": "^2.8.1"
  },
  "devDependencies": {
    "jsdom": "^27.4.0",
    "vitest": "~3.2.0"
  }
}
